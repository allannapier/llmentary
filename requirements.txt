# Core dependencies - only numpy is required
numpy

# Optional dependencies for enhanced functionality
sentence-transformers  # For semantic similarity analysis
click                 # For better CLI prompts (fallback available)
structlog             # For structured logging (fallback available)

# LLM provider dependencies (install as needed)
google-generativeai   # For Gemini integration
openai                # For OpenAI integration  
anthropic             # For Claude integration